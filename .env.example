# Copy to `.env` in the repo root and fill in values. Never commit `.env`.

# Supabase Postgres (project ref: fiozifqhhawsvvtjamfo)
# Direct (recommended for drizzle-kit push / migrations):
# DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres?sslmode=require
# Or session pooler on 5432 / transaction pooler on 6543 — see Supabase → Project Settings → Database.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.fiozifqhhawsvvtjamfo.supabase.co:5432/postgres?sslmode=require

# API server (artifacts/api-server)
JWT_SECRET=change-me-to-a-long-random-string
PORT=8080

# Vite apps (artifacts/urban-arena, artifacts/mockup-sandbox)
BASE_PATH=/
